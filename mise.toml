# See claude code environment variables documents at https://code.claude.com/docs/en/settings
[env]
GITHUB_TOKEN = "{{ exec(command='uvx --from home-secret-toml hst get github.accounts.sh.users.sh.secrets.dev.value') }}"
CODECOV_TOKEN = "{{ exec(command='uvx --from home-secret-toml hst get codecov_io.accounts.sh.users.sh.secrets.dev.value') }}"
READTHEDOCS_TOKEN = "{{ exec(command='uvx --from home-secret-toml hst get readthedocs.accounts.sh.users.sh.secrets.dev.value') }}"
PYPI_TOKEN = "{{ exec(command='uvx --from home-secret-toml hst get pypi.accounts.sh.token') }}"

[tools]
python = "3.12"
uv = "latest"
claude = "latest"

# === Tasks
[tasks.venv-create]
description = "✨ Create Python virtual environment (.venv)"
run = "uv venv"

[tasks.venv-remove]
description = "💥 Remove Python virtual environment folder (.venv)"
run = "rm -r .venv"

[tasks.inst]
description = "💾 Install Python dependencies (dev, test, doc, etc.) via uv"
run = "uv sync --all-extras"

[tasks.export]
description = "💾 Export Python dependencies to requirements*.txt via uv"
run = """
uv export --format requirements-txt --no-dev --no-emit-project --no-hashes --no-annotate --no-emit-package pywin32 --no-emit-package colorama --no-emit-package tzdata | sed 's/ ; platform_machine ==.*or.*//' > requirements.txt
uv export --format requirements-txt --extra dev --no-emit-project --no-hashes --no-annotate --no-emit-package pywin32 --no-emit-package colorama --no-emit-package tzdata | sed 's/ ; platform_machine ==.*or.*//' > requirements-dev.txt
uv export --format requirements-txt --extra test --no-emit-project --no-hashes --no-annotate --no-emit-package pywin32 --no-emit-package colorama --no-emit-package tzdata | sed 's/ ; platform_machine ==.*or.*//' > requirements-test.txt
uv export --format requirements-txt --extra doc --no-emit-project --no-hashes --no-annotate --no-emit-package pywin32 --no-emit-package colorama --no-emit-package tzdata | sed 's/ ; platform_machine ==.*or.*//' > requirements-doc.txt
"""

[tasks.test]
description = "🧪 Run Python tests with pytest"
run = ".venv/bin/pytest tests"

[tasks.cov]
description = "🧪 Run Python code coverage tests with pytest"
run = ".venv/bin/pytest tests -s --tb=native --cov=cookiecutter_pywf_open_source_demo --cov-report term-missing --cov-report html:htmlcov"

[tasks.view-cov]
description = "🌐 Open coverage report in browser (run after cov)"
run = "open htmlcov/index.html"

[tasks.build-doc]
description = "📚 Build Sphinx documentation (output: docs/build/html)"
run = ".venv/bin/sphinx-build -b html docs/source docs/build/html"

[tasks.view-doc]
description = "🌐 Open built documentation in browser"
run = "open docs/build/html/index.html"

[tasks.build]
description = "📦 Build Python wheel and source distribution"
run = "uv build"

[tasks.publish]
description = "🚀 Publish package to PyPI"
run = """
.venv/bin/python .mise/tasks/_publish.py
uv publish --token $PYPI_TOKEN
"""
