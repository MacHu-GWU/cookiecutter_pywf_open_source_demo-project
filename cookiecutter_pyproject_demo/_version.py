# Increment version before each release - follow `semantic versioning <https://semver.org/>`_
# This value has to be aligned with the one in ``pyproject.toml``
__version__ = "0.1.1"

if __name__ == "__main__":  # pragma: no cover
    print(__version__)
